type Video {
  id: ID!
  title: String!
  description: String
  url: String!
}

type Query {
  videos: [Video]
  video(id: ID!): Video
}

type Mutation {
  addVideo(title: String!, description: String, url: String!): Video
}
